const buttonElement = document.getElementById("add-to-cart-btn");

buttonElement.addEventListener("click", function (event) {
  alert("Item added to cart!");
  console.log("Item added to cart!");
});
